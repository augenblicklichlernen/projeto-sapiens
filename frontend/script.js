const API_URL = 'https://sapiens-backend-ogz2.onrender.com';
let token, userId, ytPlayer, lessonView, subjectsGrid;

document.addEventListener('DOMContentLoaded', initializeApp);

function showView(viewId) { document.querySelectorAll('.view').forEach(v => v.classList.remove('active')); const view = document.getElementById(viewId); if (view) view.classList.add('active'); }
function scrollToElement(element) { if (element) element.scrollIntoView({ behavior: 'smooth', block: 'center' }); }

async function handleLogin(e) { e.preventDefault(); const u = document.getElementById('login-username').value, p = document.getElementById('login-password').value; try { const res = await fetch(`${API_URL}/api/auth/login`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ username: u, password: p }) }); const data = await res.json(); if (!res.ok) throw new Error(data.message); localStorage.setItem('token', data.token); localStorage.setItem('username', data.username); localStorage.setItem('userId', data.userId); initializeApp(); } catch (err) { alert(`Erro no login: ${err.message}`); } }
async function handleRegister(e) { e.preventDefault(); const u = document.getElementById('register-username').value, p = document.getElementById('register-password').value; try { const res = await fetch(`${API_URL}/api/auth/register`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ username: u, password: p }) }); if (!res.ok) { const err = await res.json(); throw new Error(err.message); } alert('Registro bem-sucedido!'); showView('login-view'); } catch (err) { alert(`Erro no registro: ${err.message}`); } }

function setupUserArea() { const ua = document.getElementById('user-area'); ua.innerHTML = `<span>Olá, ${localStorage.getItem('username')}!</span><button id="logout-button">Sair</button>`; const sc = document.getElementById('score-bar-container'); if (sc) sc.style.display = 'flex'; const hrg = document.querySelector('.header-right-group'); if (hrg && !document.getElementById('my-certs-btn')) { const cb = document.createElement('button'); cb.id = 'my-certs-btn'; cb.textContent = 'Meus Certificados'; sc.insertAdjacentElement('afterend', cb); } }
async function updateScores() { const sl = document.getElementById('score-list'); try { const res = await fetch(`${API_URL}/api/content/scores/user/${userId}`, { headers: { Authorization: `Bearer ${token}` } }); if (!res.ok) throw new Error('Falha ao buscar scores'); const scores = await res.json(); sl.innerHTML = ''; if (scores?.length) { scores.forEach(s => { const p = s.total_lessons > 0 ? ((s.user_score / s.total_lessons) * 100).toFixed(0) : 0; sl.innerHTML += `<li><span class="score-name" style="color:${s.color_hex};">${s.name}</span><span class="score-value">${s.user_score}/${s.total_lessons} (${p}%)</span></li>`; }); } } catch (err) { console.error("Erro ao atualizar scores:", err); if (sl) sl.innerHTML = '<li>Erro ao carregar.</li>'; } }
async function fetchSubjects() { if (!subjectsGrid) return; try { const res = await fetch(`${API_URL}/api/content/subjects`, { headers: { Authorization: `Bearer ${token}` } }); if (!res.ok) throw new Error('Falha ao buscar matérias.'); const data = await res.json(); subjectsGrid.innerHTML = ''; if (data.main?.length) { data.main.forEach(s => { const c = document.createElement('div'); c.className = 'subject-card'; c.style.setProperty('--subject-color', s.color_hex); c.innerHTML = `<h3>${s.name}</h3>`; c.addEventListener('click', () => loadLessons(s.id, s.name)); subjectsGrid.appendChild(c); }); } let ec = document.getElementById('extra-subjects-container'); if (!ec) { ec = document.createElement('div'); ec.id = 'extra-subjects-container'; subjectsGrid.insertAdjacentElement('afterend', ec); } ec.innerHTML = ''; if (data.extra?.length) { ec.innerHTML = '<hr><h2>Matérias Extras</h2>'; data.extra.forEach(s => { const c = document.createElement('div'); c.className = 'subject-card extra'; c.style.setProperty('--subject-color', s.color_hex); c.innerHTML = `<h3>${s.name}</h3>`; c.addEventListener('click', () => loadLessons(s.id, s.name)); ec.appendChild(c); }); } } catch (err) { if (subjectsGrid) subjectsGrid.innerHTML = '<p>Erro ao carregar matérias.</p>'; } }
async function loadLessons(id, name) { if (!lessonView) return; showView('lesson-view'); lessonView.innerHTML = '<h2>Carregando...</h2>'; try { const res = await fetch(`${API_URL}/api/content/lessons/${id}`); if (!res.ok) throw new Error('Falha ao buscar lições.'); const lessons = await res.json(); let title = name ? `<h2>${name}</h2>` : ''; let btn = `<button class="back-btn" onclick="showView('subjects-view')">← Voltar</button>`; if (lessons.length === 0) { lessonView.innerHTML = `${btn}${title}<h2>Nenhuma lição.</h2>`; return; } lessonView.innerHTML = `${btn}${title}<ul class="lesson-list"></ul>`; const list = lessonView.querySelector('.lesson-list'); lessons.forEach(l => { const i = document.createElement('li'); i.className = 'lesson-item'; i.innerHTML = `<span>Lição ${l.lesson_order}: ${l.title}</span><button class="start-lesson-btn" data-lesson-id="${l.id}">Iniciar</button>`; list.appendChild(i); }); } catch (err) { lessonView.innerHTML = `<h2>Erro.</h2><p>${err.message}</p>`; } }
async function fetchReinforcementLessons() { const rs = document.getElementById('reinforcement-section'), rl = document.getElementById('reinforcement-list'); if (!rs || !rl) return; try { const res = await fetch(`${API_URL}/api/content/reinforcement/user/${userId}`, { headers: { Authorization: `Bearer ${token}` } }); const lessons = await res.json(); if (lessons.length > 0) { rs.style.display = 'block'; rl.innerHTML = ''; lessons.forEach(l => { const c = document.createElement('div'); c.className = 'subject-card reinforcement-card'; c.style.borderColor = '#4299e1'; c.innerHTML = `<h3>${l.title}</h3>`; c.dataset.lessonId = l.id; rl.appendChild(c); }); } else { rs.style.display = 'none'; } } catch (err) { console.error("Erro ao buscar reforço:", err); } }
async function renderLessonContent(id) { if (!lessonView) return; showView('lesson-view'); lessonView.innerHTML = `<h2>Carregando...</h2>`; try { const res = await fetch(`${API_URL}/api/content/lesson-detail/${id}`); const lesson = await res.json(); lessonView.innerHTML = `<button class="back-btn" onclick="loadLessons(${lesson.subject_id}, '')">← Voltar</button><div id="lesson-main-content"><h2>${lesson.title}</h2><div id="video-placeholder"></div><div id="post-video-content" style="display:none;"><hr><h3>Recursos</h3><img src="${lesson.image_url}" alt="Imagem"><br/><audio controls src="${lesson.audio_url}"></audio><br/><button id="show-text-btn">Explicação</button></div><div id="text-content" style="display:none;"><hr><h3>Explicação</h3><div>${lesson.lesson_text}</div><button id="start-quiz-btn">Questões</button></div></div><div id="quiz-content-wrapper" style="display:none;"></div>`; createYouTubePlayer(lesson); } catch (err) { lessonView.innerHTML = `<h2>Erro.</h2><p>${err.message}</p>`; } }
function createYouTubePlayer(lesson) { let i; try { const vId = new URL(lesson.video_url).searchParams.get('v'); ytPlayer = new YT.Player('video-placeholder', { height: '480', width: '100%', videoId: vId, events: { onStateChange: e => { if (e.data === YT.PlayerState.PLAYING) { i = setInterval(() => { const d = ytPlayer.getDuration(); if (d > 0 && (ytPlayer.getCurrentTime() / d) >= 0.8) { showPostVideoContent(); clearInterval(i); } }, 1000); } else clearInterval(i); } } }); } catch (e) { document.getElementById('video-placeholder').innerHTML = '<p>Vídeo indisponível.</p>'; } document.getElementById('show-text-btn')?.addEventListener('click', () => { const el = document.getElementById('text-content'); el.style.display = 'block'; scrollToElement(el); }); document.getElementById('start-quiz-btn')?.addEventListener('click', () => { document.getElementById('lesson-main-content').style.display = 'none'; renderQuiz(lesson); }); }
function showPostVideoContent() { const el = document.getElementById('post-video-content'); if (el?.style.display === 'none') { el.style.display = 'block'; scrollToElement(el); } }
async function renderQuiz(lesson) { fetch(`${API_URL}/api/content/unlock-reinforcement`, { method: "POST", headers: { "Content-Type": "application/json", Authorization: `Bearer ${token}` }, body: JSON.stringify({ userId, triggerLessonId: lesson.id }) }).then(r => r.json()).then(d => { if (d.unlocked) { showReinforcementToast(d.title); fetchReinforcementLessons(); } }); const qw = document.getElementById('quiz-content-wrapper'); qw.style.display = 'block'; qw.innerHTML = '<div id="quiz-content"></div>'; const qc = document.getElementById('quiz-content'); scrollToElement(qw); let timer, selOpt; function startTimer(d, disp, onUp) { let t = d; disp.textContent = `Tempo: ${t}s`; timer = setInterval(() => { t--; disp.textContent = `Tempo: ${t}s`; if (t <= 0) { clearInterval(timer); onUp(); } }, 1000); } function showFeedback(isOk, expl) { const fb = document.createElement('div'); fb.id = 'quiz-feedback'; fb.className = isOk ? 'correct' : 'incorrect'; fb.innerHTML = `<p>${expl}</p><button id="next-question-btn"></button>`; const cb = qc.querySelector('#confirm-answer-btn'); if (cb) { cb.style.display = 'none'; cb.insertAdjacentElement('afterend', fb); } return fb.querySelector('#next-question-btn'); } async function handleAnswer(type, correct, selected, prefix = '') { clearInterval(timer); qc.querySelectorAll('.option-btn').forEach(b => b.disabled = true); qc.querySelector('#confirm-answer-btn').disabled = true; const isOk = selected === correct; qc.querySelectorAll('.option-btn').forEach(b => { if (b.textContent === correct) b.classList.add('correct'); if (b === selOpt && !isOk) b.classList.add('incorrect'); }); if (type === 'q1') { const fbTxt = prefix + (isOk ? " Correto! Treino." : " Incorreto."); const nextBtn = showFeedback(isOk, fbTxt); nextBtn.textContent = "Questão 2"; nextBtn.addEventListener('click', renderQ2); } else if (type === 'q2') { const res = await fetch(`${API_URL}/api/content/submit-quiz`, { method: 'POST', headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${token}` }, body: JSON.stringify({ userId, lessonId: lesson.id, isCorrect: isOk, variantIndex: lesson.variantIndex }) }); const result = await res.json(); const nextBtn = showFeedback(isOk, prefix + " " + result.message); if (result.status === 'completed') { nextBtn.textContent = "Próxima Lição"; nextBtn.addEventListener('click', () => { updateScores(); loadLessons(lesson.subject_id); }); } else if (result.status === 'subject_finished') { nextBtn.textContent = "Gerar Certificado"; nextBtn.addEventListener('click', () => handleSubjectFinished(result.subjectId)); } else { nextBtn.textContent = "Voltar ao Vídeo"; nextBtn.addEventListener('click', () => renderLessonContent(lesson.id)); } } } async function renderQ2() { const pRes = await fetch(`${API_URL}/api/content/start-lesson/${lesson.id}/user/${userId}`); if (!pRes.ok) return alert('Erro de comunicação.'); const p = await pRes.json(); if (p.blocked_until && new Date() < new Date(p.blocked_until)) { alert(`Lição bloqueada. Tente em ${new Date(p.blocked_until).toLocaleTimeString()}`); return loadLessons(lesson.subject_id); } const seen = p.q2_variants_seen || []; const available = [0, 1, 2].filter(i => !seen.includes(i)); if (available.length === 0) { alert('Variantes esgotadas. Avançando.'); return loadLessons(lesson.subject_id); } const vIdx = available[Math.floor(Math.random() * available.length)]; lesson.variantIndex = vIdx; const q = lesson.q2_variants[vIdx]; const correct = q.options[0]; qc.innerHTML = `<h3>Questão 2</h3><p>${q.text}</p><div id="timer"></div><div class="options-container"></div><button id="confirm-answer-btn" disabled>Confirmar</button>`; const opts = qc.querySelector('.options-container'), timerDisp = qc.querySelector('#timer'); const onUp = () => handleAnswer('q2', correct, null, 'Tempo esgotado!'); startTimer(lesson.q2_time, timerDisp, onUp); [...q.options].sort(() => .5 - Math.random()).forEach(o => opts.innerHTML += `<button class="option-btn">${o}</button>`); selOpt = null; opts.querySelectorAll('.option-btn').forEach(b => b.addEventListener('click', () => { if (selOpt) selOpt.classList.remove('selected'); selOpt = b; b.classList.add('selected'); qc.querySelector('#confirm-answer-btn').disabled = false; })); qc.querySelector('#confirm-answer-btn').addEventListener('click', () => handleAnswer('q2', correct, selOpt?.textContent)); } function renderQ1() { const correct = lesson.q1_options[0]; qc.innerHTML = `<h3>Questão 1</h3><p>${lesson.q1_text}</p><div id="timer"></div><div class="options-container"></div><button id="confirm-answer-btn" disabled>Confirmar</button>`; const opts = qc.querySelector('.options-container'), timerDisp = qc.querySelector('#timer'); const onUp = () => handleAnswer('q1', correct, null, 'Tempo esgotado!'); startTimer(lesson.q1_time, timerDisp, onUp); [...lesson.q1_options].sort(() => .5 - Math.random()).forEach(o => opts.innerHTML += `<button class="option-btn">${o}</button>`); selOpt = null; opts.querySelectorAll('.option-btn').forEach(b => b.addEventListener('click', () => { if (selOpt) selOpt.classList.remove('selected'); selOpt = b; b.classList.add('selected'); qc.querySelector('#confirm-answer-btn').disabled = false; })); qc.querySelector('#confirm-answer-btn').addEventListener('click', () => handleAnswer('q1', correct, selOpt?.textContent)); } renderQ1(); }
async function renderReinforcementLesson(id) { if (!lessonView) return; showView('lesson-view'); lessonView.innerHTML = `<h2>Carregando...</h2>`; try { const res = await fetch(`${API_URL}/api/content/reinforcement-lesson/${id}`); if (!res.ok) throw new Error("Falha ao carregar reforço."); const rfL = await res.json(); const c = rfL.content; let html = `<button class="back-btn" onclick="showView('subjects-view')">← Voltar</button><h2>Reforço: ${rfL.title}</h2>`; if (c.video_url) html += `<iframe width="100%" height="480" src="${c.video_url.replace('watch?v=', 'embed/')}" frameborder="0" allowfullscreen></iframe>`; if (c.image_url) html += `<img src="${c.image_url}" alt="Reforço">`; if (c.audio_url) html += `<audio controls src="${c.audio_url}"></audio>`; if (c.text) html += `<div>${c.text}</div>`; if (c.questions?.length) { html += `<hr><h3>Questões de Treino</h3>`; c.questions.forEach((q, i) => { html += `<div class="quiz-question" data-correct-answer="${q.options[0]}"><h4>Questão ${i+1}</h4><p>${q.text}</p><div class="options-container">${[...q.options].sort(()=>.5-Math.random()).map(o=>`<button class="option-btn">${o}</button>`).join('')}</div><div class="rf-feedback"></div></div>`; }); } lessonView.innerHTML = html; lessonView.querySelectorAll('.quiz-question .option-btn').forEach(b => b.addEventListener('click', () => { const qb = b.closest('.quiz-question'), ca = qb.dataset.correctAnswer, sa = b.textContent, f = qb.querySelector('.rf-feedback'); qb.querySelectorAll('.option-btn').forEach(btn => btn.disabled = true); if (sa === ca) { b.classList.add('correct'); f.textContent = 'Correto!'; f.className = 'rf-feedback correct'; } else { b.classList.add('incorrect'); f.textContent = `Incorreto. Resposta: ${ca}`; f.className = 'rf-feedback incorrect'; } f.style.display = 'block'; })); } catch (err) { lessonView.innerHTML = `<h2>Erro ao carregar.</h2>`; } }
function showReinforcementToast(title) { let t = document.getElementById('reinforcement-toast'); if (!t) { t = document.createElement('div'); t.id = 'reinforcement-toast'; document.body.appendChild(t); } t.textContent = `Reforço desbloqueado: ${title}!`; t.classList.add('show'); setTimeout(() => t.classList.remove('show'), 5000); }
async function handleSubjectFinished(id) { const name = prompt("Parabéns! Você concluiu a matéria!\nInsira seu nome completo para o certificado:", ""); if (name?.trim() && name.length <= 40) { try { await fetch(`${API_URL}/api/content/generate-certificate`, { method: 'POST', headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${token}` }, body: JSON.stringify({ userId, subjectId: id, fullName: name.trim() }) }); alert('Certificado gerado!'); loadLessons(id); } catch (err) { alert("Erro ao gerar certificado."); } } else if (name) alert("Nome inválido."); }
async function loadCertificates() { showView('certificates-view'); const list = document.getElementById('certificates-list'); list.innerHTML = 'Carregando...'; try { const res = await fetch(`${API_URL}/api/content/certificates/user/${userId}`, { headers: { Authorization: `Bearer ${token}` } }); const certs = await res.json(); list.innerHTML = ''; if (certs.length === 0) { list.innerHTML = '<p>Nenhum certificado.</p>'; return; } certs.forEach(c => { const i = document.createElement('div'); i.className = 'subject-card certificate-card'; i.innerHTML = `<h3>Certificado: ${c.subject_name}</h3>`; i.dataset.certData = JSON.stringify(c); list.appendChild(i); }); } catch (e) { list.innerHTML = 'Erro ao carregar.'; } }
function showCertificate(cert) { const m = document.getElementById('certificate-modal'), c = document.getElementById('certificate-content'); c.innerHTML = `<button id="print-cert-btn" onclick="window.print()">🖨️</button><button id="close-modal-btn">X</button><div><p>Certificado de Conclusão</p><p>Certificamos que</p><p>${cert.full_name}</p><p>concluiu a matéria <strong>${cert.subject_name}</strong>,<br>pelo método Augenblicklich-Lernen.</p><p>Lições: ${cert.total_lessons}</p><p>🏅</p></div>`; m.classList.add('visible'); }

// =================================================================================
// SUBSTITUA A SUA FUNÇÃO setupEventListeners PELA VERSÃO ABAIXO
// =================================================================================

function setupEventListeners() {
    document.body.addEventListener('click', (e) => {
        const target = e.target;
        const targetId = target.id;

        // --- LÓGICA EXISTENTE (NÃO MEXER) ---
        if (targetId === 'show-register') { e.preventDefault(); showView('register-view'); }
        if (targetId === 'show-login') { e.preventDefault(); showView('login-view'); }
        if (targetId === 'login-button') { showView('login-view'); }
        if (target.classList.contains('start-lesson-btn')) { renderLessonContent(target.dataset.lessonId); }
        
        const reinforcementCard = target.closest('.reinforcement-card');
        if (reinforcementCard && reinforcementCard.dataset.lessonId) {
            renderReinforcementLesson(reinforcementCard.dataset.lessonId);
        }

        // --- LÓGICA DO SCORE E CERTIFICADO (CORRIGIDA E COMPLETA) ---
        
        // Botão "Meu Score" no cabeçalho
        if (targetId === 'score-toggle-btn') {
            const panel = document.getElementById('score-panel');
            if (panel) {
                panel.classList.toggle('visible');
                if (panel.classList.contains('visible')) {
                    updateScores();
                }
            }
        }

        // Botão "Meus Certificados" no cabeçalho
        if (targetId === 'my-certs-btn') {
            loadCertificates();
        }

        // Card de um certificado específico na lista
        const certCard = target.closest('.certificate-card');
        if (certCard && certCard.dataset.certData) {
            showCertificate(JSON.parse(certCard.dataset.certData));
        }
        
        // Botão de fechar o modal do certificado
        if (target.id === 'close-modal-btn') {
            const modal = document.getElementById('certificate-modal');
            if (modal) modal.classList.remove('visible');
        }
    });

    // Ouvinte para os formulários
    document.body.addEventListener('submit', (e) => {
        if (e.target.id === 'login-form') { handleLogin(e); }
        if (e.target.id === 'register-form') { handleRegister(e); }
    });
}